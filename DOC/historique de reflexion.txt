					[qu'est ce qu'on fait]

		Un GLPI.

il sera sur plusieurs pages (pas besoin d'async)

fonction principale de base

gestion des items

ajouter des type d'item
ajouter des item
les repertorié sur la durée
difinir leurs posision un minimum (coordonné ou encore adresse en/ou texte)

gestion des ticket
pas de compte pour l'instant
simple "requete" qui donne nom/prenom, type d'objet, raison/besoin, niveau d'urgence.


php bin/console make:form AddElement
